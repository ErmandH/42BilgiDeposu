@model IEnumerable<Ecole42Entity.Entity.Article>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section scripts{
    <script src="~/js/list-article.js"></script>
}


<div class="container ml-0 mt-0">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Makale Listesi</h4>
        </div>
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <a type="button" href="/admin/add-article" class="btn btn-primary">Veri Ekle</a>
                <div class="input-group search-area right d-lg-inline-flex d-none">
                    <input type="text" id="search-bar" class="form-control" placeholder="Arama...">
                </div>
            </div>
            <div class="table-responsive">
                <table class="table primary-table-bordered">
                    <thead class="thead-primary">
                        <tr>
                            <th scope="col">Makale Başlığı</th>
                            <th scope="col">Yazar</th>
                            <th scope="col">Yayınlama Tarihi</th>
                            <th scope="col">İŞLEMLER</th>
                        </tr>
                    </thead>
                    <tbody id="table-data">
                        @if (Model.Count() != 0)
                        {
                            foreach (var item in Model)
                            {
                                <tr role="row">
                                    <td>@item.Title</td>
                                    <td>@item.AuthorName @item.AuthorSurname</td>
                                    <td>@item.CreateDate</td>
                                    <td>
                                        <a style="color:white; cursor:pointer" class="btnDetail btn btn-primary shadow btn-xs sharp mr-1" href="/admin/update-article/@item.ID"><i class="fa fa-pencil"></i></a>
                                        <a style="color:white; cursor:pointer" class="btnDelete btn btn-danger shadow btn-xs sharp mr-1" data-id="@item.ID"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

